strip: 删除开头或结尾的字符 str.strip('chars')
split: 通过指定分隔符对字符串进行分片 str.split('chars')
replace: 把字符串中的旧字符串替换成新字符串 str.replace('old','new')

迭代器的应用: iter 和 next
list1 = [1,2,3]    # 列表
it = iter(list1)
print(next(it))
print(next(it))
print(next(it))
# print(next(it)) # except

生成器的应用: yield
def frange(start,stop,step):
      x = start
      while x < stop:
         yield x
         x += step
for i in frange(10,20,0.5):
      print(i)
      
      
items 是字典函数(dict.items),以列表方式返回元组数组


lambda表达式
使用函数定义:
def func(x,y):
    return x+y
sum = func(3,3)
print(sum)

使用 lambda 表达式:
sum = lambda x,y:x+y
print(sum(3,3))
      #简洁代码#
      
      
filter,map,reduce,zip 四个函数的用途在 func_test 中
